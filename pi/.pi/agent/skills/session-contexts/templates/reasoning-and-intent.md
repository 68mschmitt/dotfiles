---
project: {project}
theme: {lesson-theme}
created: {YYYY-MM-DD}
updated: {YYYY-MM-DD}
tags: [session-context, reasoning]
---

# Reasoning and intent: {theme title}

Read this before continuing work on {theme}. It records why things are the way
they are — the part not visible in the code or the insight docs. Items marked
`(inferred)` were read between the lines: treat them as strong hints, not facts.

## What the user was actually after

{The goal behind the literal requests: what problem prompted the session, and
what "done" looked like to the user. Label inference.}

## Decisions and why

{One block per decision that shaped the outcome. These are the questions a future
session would otherwise re-litigate.}

### {Decision, stated as what was chosen}

- Why: {the reasoning that carried it}
- Rejected: {alternative} — {why it lost}
- Signal: {what in the session settled it — the user's words, a course correction, a constraint}

## Course corrections

{Each place the user redirected the work, and what it reveals — a preference,
a constraint, or a misunderstanding not to repeat. Delete if none.}

- {what the agent did or proposed} → {how the user corrected it} → {what that reveals}

## Constraints that shaped the work

{Limits that bound the solution but were never stated as requirements:
environment, compatibility, style, risk tolerance. Label inferred ones.}

- {constraint} — {evidence for it}

## Open threads

{What was deferred or left unfinished. What a future session should pick up
first, and any warnings for it.}

- {thread} — {state it was left in, and the first step to resume}
